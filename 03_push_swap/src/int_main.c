
#include "push_swap.h"


int main(int ac, char **av)
{
	t_list *stack_a;
	t_list *stack_b;

	if(ac > 1)
	{
		if(check_input(&av[1]) == false) 	// verifie input
			exit_error(1);
		*a = init_stack(int ac, char **av); // init stack + cree
		

	}
}